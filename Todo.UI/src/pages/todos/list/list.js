import React, { useContext } from 'react'
import { UserContext } from '../../../context/userContext';
import './list.scss';

export function List() {
    

    return (
        <div>Todo works</div>
    );
}